# 霜林博弈 —— 雪林中的 3D 国际象棋

**语言**：页面的语言由用户的 query 语言决定。随仓库发出的文案是中文；
如果用户用别的语言提问，就把菜单、HUD 标签、棋谱栏抬头、toast 和结算
文案都翻成那种语言。双方名字（象牙 / 黑曜）是风味不是记谱，也要跟着翻。
走子记谱本身（`e4`、`Nf6`、`O-O`）是国际通用记号，永远保持原样。

## 加载任何东西之前先问

用 ask_user 工具问一次，包含以下问题：

1. **是否需要把网页变成全栈应用？** 目前整盘棋只在浏览器里运行——除了
   这台设备记住的设置之外什么都不存。全栈版会加服务器和数据库（即持久化
   的数据存储），让下完的对局、棋手战绩和天梯在刷新之后仍然存在，还能用
   链接分享给别人。
2. **执哪一方、对手多强？** 象牙还是黑曜，对手强度（新手 / 棋社 / 专家 /
   大师）。
3. **想要什么氛围的空地？** 默认是极光下的冬夜。换季节、换时辰只是改
   色板，不是重做。
4. **这是谁的对局？** 双方名字、首页标题和棋谱抬头都可以换成一个俱乐部、
   一场比赛或两个人的名字。

如果用户一个都不答，就直接加载默认网站文件，原样打开。

## 这是什么

一盘完整的国际象棋——规则、AI 对手、计时钟、记谱——用 three.js 渲染成
一块躺在雪林空地里的石板棋盘，天上正在下雪。

```
index.html            菜单屏和 HUD 的标记
info.md               本文件
src/
  main.js             接线：搭世界、绑指针和键盘、跑主循环
  chess/
    engine.js         0x88 棋盘、走法生成、王车易位、吃过路兵、
                      升变、将军/将杀/逼和、FEN、SAN
    ai.js             负极大值 + alpha-beta、静态搜索、杀手走法、
                      历史表、渐变棋子位置评估、四个强度等级
    worker.js         把搜索放到主线程之外跑
  game/
    match.js          回合状态、计时、吃子统计、重复局面、悔棋、PGN
    board.js          棋盘网格、格子着色器、逐格高亮数据纹理
    pieces.js         六种棋子的程序化几何
    materials.js      象牙与黑曜的着色器配方
    table.js          棋子摆台：生成、走子动画、吃子
    fx.js             复用的粒子爆发（落子溅雪、吃子飞屑）
    badges.js         可选的悬浮棋子标记
  scene/
    stage.js          渲染器、相机预设、画质档位、帧循环
    lighting.js       低角度日光、天空反弹、灯笼对、棋盘顶光
    post.js           泛光、色调映射、冷色分级、暗角、颗粒
    glsl.js           注入到多个材质里的共享噪声函数
  world/
    sky.js            天穹着色器：暮色渐变、云层、极光、星星
    ground.js         位移雪地：雪堆与视角相关闪光
    forest.js         实例化针叶树：雪盖与风摆
    snowfall.js       环绕相机循环的 GPU 降雪
    props.js          灯笼、巨石、倒木
  ui/
    menu.js           屏幕栈、键鼠等价操作、设置持久化
    hud.js            计时钟、回合条、局势条、棋谱、toast
    ui.css            整套视觉系统
vendor/               three.js 本体、用到的插件和两个字体文件
```

用任意静态服务器打开 `index.html` 即可。没有构建步骤，没有网络依赖。

可选：如果符合用户的需求，你可以使用图像和视频生成工具。

## 可以变成什么

棋规则引擎和场景是独立的两半，换掉哪一半都行。

- **另一种棋类** —— 只有 `chess/engine.js` 认识规则。`game/board.js` 用
  世界坐标画 8×8 格子；改那里和 `squareToWorld` 的格子数就行。跳棋、
  将棋、围棋都能套进同一副骨架。
- **另一种场景** —— 季节住在六个数里：`scene/lighting.js` 的太阳方向和
  颜色、`world/sky.js` 的三个天色、`scene/stage.js` 的雾、`world/ground.js`
  的雪色板。沙漠黄昏、雨夜庭院、灯笼庙院都是同一组着色器的换色。
- **打谱或出题工具** —— `match.js` 从 `fromFen()` 开局；丢给它任意 FEN
  就能从某个局面开场。棋谱栏和局势条已经在汇报引擎的判断。
- **双人同屏** —— `match.js` 只在行棋方不是 `state.human` 时才叫 worker。
  删掉这个分支就是热座模式。
- **观战棋盘** —— 用数据流驱动 `match.play()`，永不开启指针选子；动画、
  吃子特效和棋谱照样跑。

标题、双方名字、强度标签和所有提示文字都在 `index.html` 和两个 `ui/`
文件里。除了棋盘坐标——那是在 `game/board.js` 里画在 canvas 上的——
没有任何文字被烘进贴图。用户若只是看看这个网站，直接加载即可；若有特定
需求，再改这些文字或媒体内容。

## 技术浓缩

细节读源码，这里是骨架。

**一切都是程序化的。** 不加载任何网格、贴图、音频文件。棋子是用一个小型
车工语言（`at` / `line` / `quad` / `arc`）写出的旋转剖面，各自合并成
一个几何体；骑士再加一块挤出的侧面轮廓，厚度逐顶点收细，所以从任何角度
看都是雕出来的马头而不是一块板。棋盘坐标画在 canvas 上再翻转，因为贴图
的 v 轴和 canvas 是反的。

**材质是嫁接了 GLSL 的原生 three.js 材质。** 场景里每个表面都是
`MeshStandardMaterial` / `MeshPhysicalMaterial` 经过 `onBeforeCompile`
打补丁，所以保留真实的阴影、雾和光照，同时长出自己的样子：象牙的骨纹和
边缘透光、黑曜的贝壳状断口、格子上的冰封石板纹理、雪地上风刻的雪堆、
凡是世界法线朝上就积住的雪盖。共享噪声函数在 `scene/glsl.js`，注入到
`#include <common>`。

**高亮走一张 8×8 数据纹理。** 选中、合法落点、可吃子和上一步是 64 纹素
`DataTexture` 的四个通道，棋盘着色器按片元采样——没有额外网格、没有
逐格对象，圆环和圆点都由片元在格内的位置解析画出。

**雪是两套 GPU 系统。** 飘雪是一个 `Points` 缓冲，顶点着色器把雪场绕相机
回卷，所以固定粒子数也能下不完的雪；地面闪光是地面片元着色器里的哈希
晶格，一粒雪晶只有随机小面正好对齐太阳、且闪烁相位张开时才亮。

**对手在 worker 里思考。** 搜索是迭代加深的负极大值 + alpha-beta、着法
缩减、静态搜索、杀手走法和历史表，评分用中局/残局渐变的棋子位置表。
弱等级会从接近最优的着法里抽样，所以重开一局不会重样。主线程从不阻塞；
回合条显示对手在想。开局搜索二十毫秒就回话，读起来像对手压着你走，
所以 `match.js` 按等级给每步回应加了停顿——觉得节奏不对，第一张要调的
就是那张表。

**界面是单色的，从不压暗场景。** 没有强调色也没有遮罩：对比度来自白色
的透明度阶梯加上紧贴字形的投影（`--halo`），空地始终以全亮度衬在字后。
棋盘上只有两个承载含义的提示带颜色——合法落点淡蓝、可吃或被将军珊瑚红。

**画质是量出来的，不是猜的。** 像素密度先给高、阴影先开；计时一百帧，
只有中位帧真的慢，舞台才降一档，并用 toast 说明。
